// An abstract class can have implementation for some of its members whereas interface cannot have implementation for any of its members

// An interface cannot have access modifiers whereas abstract classes can have access modifiers

// An interface cannot have fields whereas abstract classes can have fields

// An interface can inherit from another interface only whereas an abstract class can inherit from interface or abstract class

// A class can inherit from multiple interfaces whereas class cannot inherit from multiple classes